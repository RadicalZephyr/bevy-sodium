[package]
name = "adr-research"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy-sodium = { path = "../../.." }

# 0003: the paired build. bevy is the incumbent arm, sodium-rust the FRP arm.
bevy = { version = "0.19.1", default-features = false }
sodium-rust = "2.1.3"
